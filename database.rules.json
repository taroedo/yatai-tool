{
  "rules": {
    ".read": false,
    ".write": false,
    "events": {
      "$eventId": {
        ".read": true,
        "settings": { ".write": true },
        "sessions": {
          "$sessionId": {
            ".write": true,
            ".validate": "newData.hasChildren(['businessDay','businessDate','status']) && (newData.child('businessDay').val() === 1 || newData.child('businessDay').val() === 2) && (newData.child('status').val() === 'open' || newData.child('status').val() === 'closed')"
          }
        },
        "transactions": {
          "$transactionId": {
            ".write": "!data.exists()",
            ".validate": "newData.hasChildren(['id','type','receiptNo','sessionId','businessDay','businessDate','registerId','cashierId','deviceId','clientTime','yakisobaQty','frankfurterQty','total','received','change','serverTime']) && newData.child('id').val() === $transactionId && (newData.child('type').val() === 'sale' || newData.child('type').val() === 'cancel') && (newData.child('registerId').val() === 'R1' || newData.child('registerId').val() === 'R2') && (newData.child('cashierId').val() === 'C1' || newData.child('cashierId').val() === 'C2') && newData.child('deviceId').isString() && newData.child('deviceId').val().length >= 10 && newData.child('yakisobaQty').isNumber() && newData.child('frankfurterQty').isNumber() && newData.child('yakisobaQty').val() >= 0 && newData.child('frankfurterQty').val() >= 0 && newData.child('total').val() === newData.child('yakisobaQty').val() * 300 + newData.child('frankfurterQty').val() * 200 && newData.child('serverTime').val() <= now + 10000"
          }
        },
        "audit": { "$auditId": { ".write": "!data.exists()" } },
        "presence": { "$deviceId": { ".write": true } }
      }
    }
  }
}
